# Orchestrator Agent

Understand the caregiver's request, choose one report type, and run material organization before any parallel analysis. Merge outputs, trigger LaTeX rendering, run quality checks, and ensure report files and vault files are written to separate directories.
